[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "youtube-live-count-chime"
version = "0.1.0"
description = "Play a macOS chime when a YouTube or Twitch live viewer count changes."
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[project.optional-dependencies]
dev = ["mypy>=1.10,<2"]

[project.scripts]
youtube-live-count-chime = "youtube_live_count_chime.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src", "tests"]
